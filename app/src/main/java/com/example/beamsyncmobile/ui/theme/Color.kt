package com.example.beamsyncmobile.ui.theme

import androidx.compose.ui.graphics.Color

// ──────────────────────────────────────────────
// BeamSync Industrial Brutalism Color System
// ──────────────────────────────────────────────

object BeamsyncColors {
    // Surface colors
    val surfaceBase = Color(0xFF0A0A0A)
    val surfaceRaised = Color(0xFF141414)
    val surfaceHigher = Color(0xFF1E1E1E)
    val overlayDim = Color(0x99000000)

    // Accent colors
    val accentPrimary = Color(0xFFE4F900)
    val accentSecondary = Color(0xFF00E5FF)
    val surfacePositive = Color(0xFF00FF88)
    val surfaceCritical = Color(0xFFFF1A4F)

    // Text colors
    val textPrimary = Color(0xFFF0F0F0)
    val textSecondary = Color(0xFF888888)
    val textDisabled = Color(0xFF444444)
    val textAccent = Color(0xFFE4F900)
    val textAccentCyan = Color(0xFF00E5FF)
    val textError = Color(0xFFFF1A4F)

    // Stroke colors
    val strokeDefault = Color(0xFF2A2A2A)
    val strokeActive = Color(0xFFE4F900)
}
